import 'package:flutter/material.dart';

class MapPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('خريطة توضح مواقع الدوريات النشطة (قيد التطوير)', style: TextStyle(fontSize: 16)),
    );
  }
}